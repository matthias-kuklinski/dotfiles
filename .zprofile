# Environment Variables
export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-agent.socket" # Provide bind address socket for the ssh-agent on boot.
export ZDOTDIR=~/.config/zsh
export STARSHIP_CONFIG=~/.config/starship/config.toml
